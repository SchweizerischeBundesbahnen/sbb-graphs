import 'dart:convert';

import 'package:graph/graph.dart';

class DOTEncoder extends Converter<Graph, String> {
  const DOTEncoder();

  @override
  String convert(Graph input) {
    throw UnimplementedError();
  }
}
